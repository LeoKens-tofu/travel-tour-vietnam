mixin buildTree(list, level = 0) 
  each item in list
    - const prefix = "--".repeat(level)
    option(value=item.id) #{prefix} #{item.name}
    if (item.child && item.child.length > 0) 
      +buildTree(item.child, level + 1)